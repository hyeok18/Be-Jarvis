# Be Jarvis Repository Instructions

이 파일은 저장소 전체에 적용되는 Codex 작업 규칙이다. 사용자의 명시적 지시가 이 문서보다 우선한다.

## 1. 기준 문서와 우선순위

작업을 시작하기 전에 다음 파일을 순서대로 읽는다.

1. `AGENTS.md`
2. `be-jarvis-team-handoff/START_HERE.md`
3. `be-jarvis-team-handoff/docs/DEVELOPMENT_PRIORITY.md`
4. `be-jarvis-team-handoff/docs/PRD.md`
5. `be-jarvis-team-handoff/docs/VISION_ROADMAP.md`
6. `be-jarvis-team-handoff/docs/development-logs/INDEX.md`
7. 현재 작업 단위와 관련된 최신 개발일지

요구사항이 충돌할 때는 다음 순서로 판단한다.

```text
사용자의 현재 명시적 지시
→ 현재 목적지 정부의 공식 출처
→ PRD.md
→ VISION_ROADMAP.md
→ DEVELOPMENT_PRIORITY.md
→ 이전 개발일지
→ 기존 참고 코드
```

비자·입국 규정은 변경 가능성이 큰 고위험 정보다. 현재 공식 출처를 확인하지 못한 내용은 추측해 구현하거나 사용자에게 확정적으로 노출하지 않는다.

## 2. 세션 시작 절차

모든 새 작업 세션에서 다음을 수행한다.

1. `git status`로 기존 변경을 확인하고 다른 팀원의 작업을 보존한다.
2. `DEVELOPMENT_PRIORITY.md`에서 `진행 중` 또는 가장 앞선 `대기` 작업 단위를 찾는다.
3. `development-logs/INDEX.md`와 해당 작업 단위의 최신 일지를 읽는다.
4. 이전 일지의 `다음 작업에서 해야 할 일`, 미해결 문제, 테스트 결과를 확인한다.
5. 선행 작업이 완료됐는지 확인한 뒤 한 개의 작업 단위만 활성화한다.
6. 작업 시작 전 이번 세션의 범위와 완료 조건을 짧게 사용자에게 알린다.

이전 작업이 `진행 중` 또는 `막힘`이면 새 작업 단위를 시작하지 말고 해당 작업을 먼저 이어간다.

## 3. 작업 단위 운영 규칙

- 작업 단위 ID는 `DEVELOPMENT_PRIORITY.md`의 `WU-XX`를 사용한다.
- 동시에 두 개 이상의 작업 단위를 구현하지 않는다.
- 현재 단위의 선행 조건, 산출물, 테스트, 완료 조건을 확인한 후 코드를 수정한다.
- 범위 밖에서 발견한 개선점은 즉시 구현하지 않고 개발일지의 후속 작업에 기록한다.
- P1 기능 때문에 P0 기능의 구현 또는 검증을 늦추지 않는다.
- AI 기능보다 AI 없이 완료 가능한 기본 사용자 흐름을 먼저 보장한다.
- 공식 정보가 필요한 작업은 확인한 URL과 확인 날짜를 문서 또는 코드 메타데이터에 남긴다.
- 실제 개인정보, 실제 여권정보, 실제 신청정보를 테스트 데이터로 사용하지 않는다.

## 4. 완료 게이트

작업 단위는 다음 조건을 모두 만족해야 `완료`다.

1. 단위에 정의된 구현 산출물이 존재한다.
2. 관련 자동 테스트가 통과한다.
3. 자동화하기 어려운 화면·접근성·보안 조건은 수동 검증 결과가 있다.
4. 실패 경로와 복구 경로를 확인했다.
5. 테스트 과정에서 실제 개인정보 또는 비밀값을 저장·출력하지 않았다.
6. `DEVELOPMENT_PRIORITY.md`의 상태와 완료 증거를 갱신했다.
7. 아래 규칙에 맞는 개발일지와 `development-logs/INDEX.md`를 갱신했다.

테스트를 실행하지 않았거나 실패했다면 완료로 표시하지 않는다. `작동할 것 같다`는 완료 증거가 아니다.

## 5. 개발일지 자동 작성 규칙

### 저장 위치

모든 개발일지는 아래 폴더에 Markdown으로 저장한다.

```text
be-jarvis-team-handoff/docs/development-logs/
```

파일명은 다음 형식을 사용한다.

```text
YYYY-MM-DD_WU-XX_short-slug.md
```

같은 작업 단위가 여러 세션에 걸치면 새 파일을 무분별하게 만들지 말고 기존 파일에 `세션 업데이트` 섹션을 날짜순으로 추가한다. 서로 다른 날짜의 독립적인 재작업이나 회귀 수정은 새 파일을 만들고 `INDEX.md`에서 서로 연결한다.

### 언제 작성하는가

- 구현과 테스트가 완료되어 작업 단위를 `완료`로 바꾸기 직전에 반드시 작성한다.
- 세션이 끝나지만 작업 단위가 미완료이면 `진행 중` 상태로 작성 또는 갱신한다.
- 사용자 입력, 외부 권한, 공식 자료 접근 문제로 멈추면 `막힘` 상태로 작성 또는 갱신한다.
- 문서·규칙·데이터 스키마처럼 코드가 아닌 작업도 하나의 작업 단위라면 동일하게 기록한다.

### 필수 내용

각 일지는 `DEVELOPMENT_LOG_TEMPLATE.md` 형식을 따르고 다음 질문에 구체적으로 답해야 한다.

- 무엇을 만들었는가?
- 무엇이 문제였고 어디에서 막혔는가?
- 문제를 어떻게 해결했는가?
- 어떤 테스트와 검증을 실행했고 결과는 무엇인가?
- 변경된 파일은 무엇인가?
- 남은 위험이나 미해결 항목은 무엇인가?
- 다음 작업에서는 무엇을 먼저 해야 하는가?

문제가 없었던 항목도 생략하지 말고 `없음`이라고 적는다. 실행하지 않은 테스트는 `미실행`과 이유를 적는다. 명령어, 테스트 수, 성공·실패 결과는 가능한 한 정확히 남긴다.

### 인덱스 갱신

일지를 만들거나 갱신할 때 `development-logs/INDEX.md`에 다음을 함께 반영한다.

- 작업 단위 ID와 제목
- 상태: `대기`, `진행 중`, `막힘`, `완료`
- 최신 기록 날짜
- 개발일지 링크
- 다음 작업 단위 또는 재개 지점

## 6. 세션 종료와 다음 세션 인계

한 작업 단위가 구현·테스트·검증을 통과하면 다음 순서를 지킨다.

1. 관련 문서와 테스트 결과를 정리한다.
2. 개발일지를 작성한다.
3. `development-logs/INDEX.md`를 갱신한다.
4. `DEVELOPMENT_PRIORITY.md`에서 현재 단위를 `완료`, 다음 단위를 `대기` 또는 `다음`으로 표시한다.
5. 사용자에게 완료 내용, 검증 결과, 다음 작업 단위 ID를 보고한다.
6. 사용자가 여러 작업 단위를 한 번에 계속하라고 명시하지 않았다면 다음 단위 구현을 시작하지 않고 세션을 종료한다.

다음 세션은 이전 일지를 읽고 `다음 작업에서 해야 할 일`의 첫 항목부터 시작한다.

## 7. 안전·품질 불변 조건

- 공개 데모에서는 가상 정보만 사용한다.
- 신청값을 서버 파일, 데이터베이스, 분석 로그에 저장하지 않는다.
- AI 미동의 사용자의 신청값을 서버나 외부 API로 전송하지 않는다.
- AI 제안은 사용자의 명시적 승인 전까지 원문을 변경하지 않는다.
- 승인 가능성, 입국 보장, 법률 자문으로 해석되는 표현을 사용하지 않는다.
- 미국 정부·CBP·대사관의 공식 서비스로 오인될 로고와 표현을 사용하지 않는다.
- 비밀키와 `.env`를 커밋하거나 개발일지에 기록하지 않는다.
- 관련 없는 기존 변경을 삭제하거나 되돌리지 않는다.

## 8. 문서 동기화

제품 범위, 수용 기준, KPI가 바뀌면 관련 문서를 함께 갱신한다.

- 세부 기능·수용 기준 변경: `PRD.md`
- 문제 우선순위·KPI·확장 순서 변경: `VISION_ROADMAP.md`
- 작업 순서·상태 변경: `DEVELOPMENT_PRIORITY.md`
- 실제 구현 과정과 검증 결과: `development-logs/`
- 새 팀원 인수인계 핵심 변경: `START_HERE.md`

문서의 상태와 실제 구현이 다르면 개발일지에 차이를 명시하고, 같은 작업 단위 안에서 가능한 범위까지 동기화한다.
