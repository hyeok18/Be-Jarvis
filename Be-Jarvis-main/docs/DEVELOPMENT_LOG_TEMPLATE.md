# [WU-XX] 작업 제목

| 항목 | 내용 |
|---|---|
| 작업 단위 | WU-XX |
| 상태 | 진행 중 / 막힘 / 완료 |
| 작업일 | YYYY-MM-DD |
| 담당 | A1 / A2 / B1 / B2 / 공통 |
| 대상 AC | AC-XX |
| 기준 문서 | [PRD](../PRD.md), [개발 우선순위](./DEVELOPMENT_PRIORITY.md) |
| 선행 작업 | WU-XX |
| 다음 작업 단위 | WU-XX 또는 재개 조건 |

## 1. 이번 작업의 목표

- 해결하려는 문제:
- 세션 범위:
- 완료 조건:
- 범위 밖 항목:

## 2. 무엇을 만들었는가

- 구현 또는 문서화한 내용:
- 사용자 또는 시스템 동작 변화:

변경한 파일:

- `path/to/file`

## 3. 무엇이 문제였고 어디에서 막혔는가

- 문제:
- 막힌 지점:
- 영향:

문제가 없었다면 `없음`이라고 적는다.

## 4. 어떻게 해결했는가

- 원인:
- 선택한 해결 방법:
- 다른 선택지를 쓰지 않은 이유:

해결하지 못했다면 시도한 내용과 정확한 재개 조건을 적는다.

## 5. 테스트와 검증

| 검증 항목 | 실행 방법·명령 | 결과 | 증거 또는 비고 |
|---|---|---|---|
| 관련 단위 테스트 | `npm test -- ...` | 성공 / 실패 / 미실행 | 테스트 수와 핵심 결과 |
| 정적 검사 | `npm run lint`, `npm run typecheck` | 성공 / 실패 / 미실행 | 오류 수 |
| 빌드 | `npm run build` | 성공 / 실패 / 미실행 | 대상 환경 |
| 수동 AC 검증 | PRD의 재현 절차 | 성공 / 실패 / 미실행 | 화면, 계정 유형, viewport 등 |
| 실패·복구 경로 | 오류 mock 또는 차단 조건 | 성공 / 실패 / 미실행 | 마지막 정상 상태 유지 여부 |

- 통과한 AC:
- 실패한 AC:
- 미실행 테스트와 이유:
- 테스트 데이터 안전 확인: 합성 데이터만 사용 / 해당 없음
- 비밀값 노출 확인: 없음 / 발견 및 조치

테스트를 실행하지 않았거나 실패했다면 상태를 `완료`로 쓰지 않는다.

## 6. 변경된 파일

| 파일 | 변경 이유 |
|---|---|
| `path/to/file` | 설명 |

## 7. 남은 위험과 미해결 항목

- 남은 위험:
- 후속 작업 후보:
- 사용자 또는 외부 입력이 필요한 사항:

없으면 각 항목에 `없음`이라고 적는다.

## 8. 다음 작업에서는 어떻게 해야 하는가

1. 다음 세션에서 가장 먼저 할 일
2. 반드시 다시 확인할 테스트 또는 결정
3. 다음 작업 단위로 넘어가기 위한 조건

## 9. 세션 업데이트

같은 작업 단위를 여러 세션에서 이어가면 아래 형식으로 날짜순 추가한다.

### YYYY-MM-DD

- 추가 구현:
- 새 문제 또는 막힘:
- 해결 또는 시도:
- 검증 결과:
- 현재 재개 지점:
